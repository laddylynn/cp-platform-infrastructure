<source>
  @type tail

  # Parse the timestamp, but still collect the entire line as 'message'
  # format /^(?<message>(?<time>[^ ]*\s*[^ ]* [^ ]*) .*)$/
  format none

  path  /home/coolbitx/.litecoin/testnet4/debug.log
  pos_file /var/lib/google-fluentd/pos/litecoin.pos
  read_from_head true
  tag litecoin
</source>